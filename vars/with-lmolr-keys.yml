tmux:
  keys:
    clear_scrollback: 'C-l'

    # Copycat.
    copycat:
      search: 'C-/'

    # Layouts.
    layout:
      next: 'Space'

    # Windows.
    window:
      new:    'C-w'
      choose: 'w'
      kill:   'M-w'

    # Sessions.
    session:
      new:    'C-s'
      detach: 'd'
      go:
        next: 'C-p'
        prev: 'C-n'

    # Panes.
    pane:
      maximize_toggle: 'C-Space'
      swap:
        up:    'K'
        down:  'J'
        left:  'H'
        right: 'L'
      resize:
        up:    'C-k'
        down:  'C-j'
        left:  'C-h'
        right: 'C-l'
      split:
        horizontal:
          upper: 'S'
          lower: '-'
        vertical:
          left:  'V'
          right: '/'
      go:
        up:    'k'
        down:  'j'
        left:  'h'
        right: 'l'

    # Status.
    status_toggle: '`'
